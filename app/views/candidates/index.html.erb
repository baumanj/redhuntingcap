<strong>Candidates to act on</strong>
<ul>
  <% @todo.each do |candidate| %>
  <!-- TODO Move to partial later -->
  <li>
    <%= link_to candidate.alias, candidate %>
    <%= mail_to candidate.email %>
    <%= candidate.phone %>
    Next step
  </li>
  <% end %>
</ul>
<strong>Candidates I am involved with</strong>
<ul>
  <% @other.each do |candidate| %>
  <li>
    <%= link_to candidate.alias, candidate %>
    <%= mail_to candidate.email %>
    <%= candidate.phone %>
    Next step
  </li>
  <% end %>
</ul>
